- title @theme.name

- if !@theme.categories.blank?
  - for categorie in @theme.categories
    = link_to h(categorie.name), categorie
- else
  Pas de catégories pour ce thème

%p
  = link_to "Edit", edit_theme_path(@theme)
  |
  = link_to "Destroy", @theme, :confirm => 'Are you sure?', :method => :delete
  |
  = link_to "View All", themes_path
